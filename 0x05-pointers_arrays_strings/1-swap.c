#include "main.h"

/**
* swap_int - swaps two integer's value
* @a: first integer
* @b: second integer
* Return: returns nothing
*/

void swap_int(int *a, *b)
{
	int tmp = *a;

	*a = *b;
	*b = tmp;
}
